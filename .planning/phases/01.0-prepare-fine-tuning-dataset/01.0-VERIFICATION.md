---
phase: 01.0-prepare-fine-tuning-dataset
verified: 2026-04-05T17:53:55Z
status: human_needed
score: 3/3 must-haves verified
re_verification:
  previous_status: human_needed
  previous_score: 3/3
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Run prepare_splits.py on HPC and verify fs_train.csv has ~21K rows and fs_eval.csv has ~5.3K rows"
    expected: "Script completes, CSVs written with correct row counts, zero speaker overlap confirmed in output"
    why_human: "Data files live on HPC at /users/PAS2030/srishti/asr_fairness/data -- cannot access from verification environment"
  - test: "Run validate_splits.py on the generated splits and confirm exit code 0"
    expected: "All 7 checks pass, exit code 0, no FAIL lines in output"
    why_human: "Validation requires the generated CSV files and audio files on HPC filesystem"
  - test: "Run data_loader smoke test: python scripts/training/data_loader.py --manifest /path/to/fs_train.csv --axis ethnicity --batch-size 4"
    expected: "Prints audio shape, audio lengths, transcripts, demographic groups without errors"
    why_human: "Requires real audio files and soundfile library on HPC to test actual audio loading and resampling"
---

# Phase 1.0: Prepare Fine-Tuning Dataset Verification Report

**Phase Goal:** Train and eval splits with demographic labels are ready for fine-tuning, with data loaders that yield (audio, transcript, demographic_group) tuples
**Verified:** 2026-04-05T17:53:55Z
**Status:** human_needed
**Re-verification:** Yes -- re-verification after previous human_needed (no gaps existed; re-confirming all artifacts)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Fair-Speech and Common Voice 24 train/eval splits are created with demographic labels (ethnicity, accent) preserved | VERIFIED | `prepare_splits.py` (297 lines) uses `StratifiedGroupKFold(n_splits=5)` on ethnicity axis with speaker-level grouping (line 102-107), saves fs_train.csv/fs_eval.csv with all demographic columns plus speaker_id (line 278-279). CV test manifest referenced as-is per D-02 (lines 180-202, `check_cv_manifest`). |
| 2 | Data loaders yield batches of (audio_tensor, reference_transcript, demographic_group) suitable for training | VERIFIED | `ASRFairnessDataset.__getitem__` returns dict with `audio`, `transcript`, `demographic_group`, `utterance_id` keys (lines 76-96). `collate_fn` pads audio and returns batched tensors with `audio_lengths` (lines 101-133). `create_dataloader` factory wires Dataset + Sampler + collate into a DataLoader (lines 209-266). |
| 3 | Splits are balanced enough that each demographic group has sufficient representation for per-group metrics | VERIFIED | `prepare_splits.py` checks MIN_GROUP_SIZE=50 in eval (lines 152-170). `validate_splits.py` Check 2 enforces MIN_GROUP_SIZE per axis per split (lines 94-119). `DemographicStratifiedSampler` oversamples small groups to MIN_GROUP_SIZE floor (lines 175-185). |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/prepare_splits.py` | Fair-Speech speaker-level stratified train/eval split generation (min 100 lines) | VERIFIED | 296 lines, syntax valid, StratifiedGroupKFold, speaker discovery from raw TSV, argparse CLI, zero-overlap assert |
| `scripts/validate_splits.py` | Split validation with demographic checks, leakage detection, optional plots (min 120 lines) | VERIFIED | 502 lines, syntax valid, 7 checks (demographic completeness, group size, speaker leakage, utterance leakage, audio existence, text normalization, distribution drift), --plots flag, exit codes 0/1 |
| `scripts/training/__init__.py` | Package marker for training module (min 1 line) | VERIFIED | 7 lines, imports all 4 public symbols (ASRFairnessDataset, DemographicStratifiedSampler, collate_fn, create_dataloader) |
| `scripts/training/data_loader.py` | ASRFairnessDataset, DemographicStratifiedSampler, collate_fn, create_dataloader (min 180 lines) | VERIFIED | 293 lines, syntax valid, all 4 exports present as top-level definitions, soundfile audio loading, demographic stratification, oversampling, padded collation, CLI smoke test block |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| prepare_splits.py | fs_manifest.csv | pandas read_csv | WIRED | Line 256: `df = pd.read_csv(args.fs_manifest)` |
| prepare_splits.py | fairspeech_metadata.tsv | csv.DictReader | WIRED | Line 248: `meta_path = os.path.join(FS_DIR, "fairspeech_metadata.tsv")`, opened with DictReader at line 46 |
| validate_splits.py | fs_train.csv / fs_eval.csv | pandas read_csv | WIRED | Lines 449-450: `pd.read_csv(args.train)` and `pd.read_csv(args.eval)` |
| data_loader.py | manifest CSVs | pandas read_csv in __init__ | WIRED | Line 51: `self.df = pd.read_csv(manifest_csv)` |
| data_loader.py | soundfile | sf.read() in __getitem__ | WIRED | Line 80: `audio, sr = sf.read(row["audio_path"], dtype="float32")` |
| data_loader.py | torch.utils.data.DataLoader | create_dataloader factory | WIRED | Line 258: `return DataLoader(dataset, ...)` with collate_fn, sampler, pin_memory |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| data_loader.py | self.df | pd.read_csv(manifest_csv) | Yes -- reads CSV manifests produced by prepare_splits.py | FLOWING |
| data_loader.py | audio tensor | sf.read(audio_path) | Yes -- reads real audio files from filesystem | FLOWING |
| data_loader.py | demographics | self.df[demographic_axis].fillna("") | Yes -- reads from CSV column, not hardcoded | FLOWING |
| prepare_splits.py | df | pd.read_csv(args.fs_manifest) | Yes -- reads manifest CSV from disk | FLOWING |
| prepare_splits.py | speaker_mapping | csv.DictReader on fairspeech_metadata.tsv | Yes -- reads raw TSV, builds hash_name->speaker_id dict | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| prepare_splits.py syntax | `python3 -c "import ast; ast.parse(open('scripts/prepare_splits.py').read())"` | SYNTAX OK | PASS |
| validate_splits.py syntax | `python3 -c "import ast; ast.parse(open('scripts/validate_splits.py').read())"` | SYNTAX OK | PASS |
| data_loader.py syntax | `python3 -c "import ast; ast.parse(open('scripts/training/data_loader.py').read())"` | SYNTAX OK | PASS |
| __init__.py syntax | `python3 -c "import ast; ast.parse(open('scripts/training/__init__.py').read())"` | SYNTAX OK | PASS |
| data_loader.py exports | AST top-level extraction | Classes: ASRFairnessDataset, DemographicStratifiedSampler; Functions: collate_fn, create_dataloader | PASS |
| No torchaudio.load calls | grep for torchaudio.load in data_loader.py | Only in comments (lines 9, 33, 79), never called as code | PASS |
| Commit 3c14450 (prepare_splits) | git log --oneline | Exists: "feat(01.0-01): create prepare_splits.py for Fair-Speech speaker-level stratified splitting" | PASS |
| Commit 15094c3 (validate_splits) | git log --oneline | Exists: "feat(01.0-01): create validate_splits.py with 7 validation checks and optional plots" | PASS |
| Commit 2aafca8 (Dataset+collate) | git log --oneline | Exists: "feat(01.0-02): create ASRFairnessDataset and collate_fn" | PASS |
| Commit f652fcf (Sampler+factory) | git log --oneline | Exists: "feat(01.0-02): add DemographicStratifiedSampler and create_dataloader factory" | PASS |
| Smoke test with real data | Requires HPC filesystem | N/A | SKIP -- no local audio files |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| DATA-01 | 01.0-01 | Fair-Speech and Common Voice 24 train/eval splits created with demographic labels preserved | SATISFIED | prepare_splits.py creates speaker-stratified splits preserving ethnicity, gender, age, first_language, speaker_id. CV test manifest referenced as-is (check_cv_manifest function). |
| DATA-02 | 01.0-02 | Data loaders yield batches of (audio_tensor, reference_transcript, demographic_group) for training | SATISFIED | ASRFairnessDataset.__getitem__ returns dict with audio/transcript/demographic_group. collate_fn produces padded batches. create_dataloader provides factory with stratified and sequential modes. |
| DATA-03 | 01.0-01, 01.0-02 | Each demographic group has sufficient representation in both train and eval splits for per-group metrics | SATISFIED | MIN_GROUP_SIZE=50 enforced by prepare_splits.py (warning output), validate_splits.py (Check 2, issues/warnings), and DemographicStratifiedSampler (oversampling floor). |

No orphaned requirements found -- all 3 requirements mapped to Phase 1.0 in REQUIREMENTS.md traceability table are covered by the plans.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | - | - | No TODO/FIXME/placeholder/stub patterns found in any phase artifact |

### Human Verification Required

### 1. End-to-end split generation on HPC

**Test:** Run `python scripts/prepare_splits.py` on the HPC cluster where Fair-Speech data resides
**Expected:** Script completes successfully, fs_train.csv has ~21K rows, fs_eval.csv has ~5.3K rows, zero speaker overlap confirmed, all ethnicity groups have 50+ utterances in eval
**Why human:** Data files live on HPC at /users/PAS2030/srishti/asr_fairness/data -- cannot access from verification environment

### 2. Validation script passes on generated splits

**Test:** Run `python scripts/validate_splits.py` after generating the splits
**Expected:** All 7 checks pass (exit code 0), no FAIL lines in output
**Why human:** Requires the generated CSV files and actual audio files on HPC filesystem

### 3. Data loader smoke test with real audio

**Test:** Run `python scripts/training/data_loader.py --manifest /path/to/fs_train.csv --axis ethnicity --batch-size 4`
**Expected:** Prints audio shape (e.g., torch.Size([4, N])), audio lengths, sample transcripts, demographic groups without errors
**Why human:** Requires real audio files and soundfile/torchaudio libraries on HPC to test actual audio loading and resampling

### Gaps Summary

No code-level gaps found. All 4 artifacts exist, are substantive (293-502 lines each for the main scripts), are properly wired (all 6 key links verified), and contain no stubs, TODOs, or placeholders. All 3 roadmap success criteria are satisfied at the code level. All 3 requirement IDs (DATA-01, DATA-02, DATA-03) are covered.

The only remaining verification is runtime execution on the HPC cluster where the actual data resides. This is expected for a data preparation phase -- the scripts cannot be tested end-to-end without access to the Fair-Speech audio files and metadata.

---

_Verified: 2026-04-05T17:53:55Z_
_Verifier: Claude (gsd-verifier)_
