---
phase: 01.0-prepare-fine-tuning-dataset
plan: 01
subsystem: data-prep
tags: [fair-speech, splitting, stratified, sklearn, validation, demographics]

# Dependency graph
requires: []
provides:
  - "scripts/prepare_splits.py -- Fair-Speech speaker-level stratified train/eval split generation"
  - "scripts/validate_splits.py -- Split validation with 7 demographic checks and optional plots"
  - "fs_train.csv and fs_eval.csv output manifests with speaker_id column"
affects: [01.1-lora-prototype, 01.2-standard-lora-baseline, 02-grpo-training]

# Tech tracking
tech-stack:
  added: [sklearn.model_selection.StratifiedGroupKFold]
  patterns: [speaker-level stratified splitting, validation issue collection with warnings/issues separation]

key-files:
  created:
    - scripts/prepare_splits.py
    - scripts/validate_splits.py
    - logs/prepare-splits-creation.md
    - logs/validate-splits-creation.md
  modified: []

key-decisions:
  - "Used StratifiedGroupKFold(n_splits=5) instead of GroupShuffleSplit to guarantee both speaker disjointness and ethnicity stratification"
  - "Speaker ID discovered at runtime from fairspeech_metadata.tsv by finding column with ~593 unique values"
  - "Validation separates issues (exit 1) from warnings (informational) for graduated severity"

patterns-established:
  - "Speaker ID discovery: runtime column cardinality inspection of raw TSV"
  - "Validation pattern: 7 structured checks with issues/warnings separation, matching existing validate_*.py style"
  - "Diagnostic plots behind --plots flag for optional matplotlib generation"

requirements-completed: [DATA-01, DATA-03]

# Metrics
duration: 3min
completed: 2026-04-05
---

# Phase 1.0 Plan 01: Fair-Speech Train/Eval Splits Summary

**Speaker-level stratified 80/20 Fair-Speech splitting with StratifiedGroupKFold and 7-check validation script**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-05T17:42:16Z
- **Completed:** 2026-04-05T17:45:23Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Created prepare_splits.py that discovers speaker IDs from raw TSV at runtime, performs StratifiedGroupKFold 80/20 split preserving ethnicity distribution, and saves fs_train.csv / fs_eval.csv with all demographic columns plus speaker_id
- Created validate_splits.py with 7 validation checks (demographic completeness, MIN_GROUP_SIZE, speaker leakage, utterance leakage, audio existence, text normalization, distribution drift) plus optional matplotlib diagnostic plots
- Both scripts follow existing codebase patterns: hardcoded HPC paths, module-level constants, argparse CLI, validation issue collection

## Task Commits

Each task was committed atomically:

1. **Task 1: Create prepare_splits.py** - `3c14450` (feat)
2. **Task 2: Create validate_splits.py** - `15094c3` (feat)

## Files Created/Modified
- `scripts/prepare_splits.py` - Fair-Speech speaker-level stratified train/eval split generator (296 lines)
- `scripts/validate_splits.py` - Split validation with 7 checks and optional plots (502 lines)
- `logs/prepare-splits-creation.md` - Log entry for prepare_splits.py creation
- `logs/validate-splits-creation.md` - Log entry for validate_splits.py creation

## Decisions Made
- Used StratifiedGroupKFold(n_splits=5) over GroupShuffleSplit to guarantee both speaker disjointness and ethnicity distribution preservation (per RESEARCH.md Pitfall 2)
- Speaker column discovered at runtime by finding the column with ~500-700 unique values in fairspeech_metadata.tsv, rather than hardcoding a column name (per RESEARCH.md Pitfall 1)
- Validation script separates issues (critical, cause exit 1) from warnings (informational, exit 0) -- enhancement over the simpler existing validate_*.py pattern

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None - both scripts are complete implementations ready for execution on the HPC cluster.

## Next Phase Readiness
- prepare_splits.py ready to run on HPC to generate fs_train.csv and fs_eval.csv
- validate_splits.py ready to validate the generated splits
- Phase 1.1 (LoRA prototype) and 1.2 (standard LoRA baseline) can proceed once splits are generated
- CV test manifest (cv_test_manifest.csv) is verified as-is per D-02

## Self-Check: PASSED

All files found, all commits verified.

---
*Phase: 01.0-prepare-fine-tuning-dataset*
*Completed: 2026-04-05*
